// Backwards-compatible re-exports. Prefer importing from `lib/supabaseClient`
// (browser) or `lib/supabaseService` (server) directly.
export { getSupabaseClient } from './supabaseClient'
export { getSupabaseServiceClient } from './supabaseService'

